export default function LoginPage() {
  return (
    <main className="container">
      <div className="card">
        <h1>Login Disabled (Local Mode)</h1>
        <p className="muted">
          Authentication is currently disabled so you can focus on local UI/content
          development first.
        </p>
      </div>
    </main>
  );
}
